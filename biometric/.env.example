# Base URL of the AcademicX server (no trailing /api).
# Local dev:  http://localhost:5000
# Production: your Render server URL, e.g. https://academicx-server.onrender.com
#
# NOTE: WebAuthn (fingerprint) requires HTTPS. Vercel provides HTTPS automatically.
VITE_API_URL=http://localhost:5000
